# Метаданные пакета test
$package:
  seaf-core: # Идентификатор пакета
    # Краткое название пакета
    name: SEAF
    # Поставщик
    vendor: Sber
    # Описание
    description: Sber Enterprise Architect Framework
    # Версия
    version: 1.2.1
    # Зависимости от других пакетов
    dependencies:
      dochub: ">=3.8.0"

imports:
  - functions/root.yaml
  - entities/root.yaml
  - docs/root.yaml
