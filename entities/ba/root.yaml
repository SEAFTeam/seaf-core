imports:

# Для работы метамодели БА и всех сопуствующих элементов необходимо,
# чтобы расширение Hexagon было подключено (см. корневой манифест SEAF)

  # seaf.ba.ttl objects representing BA Metamodel
  - metamodel.yaml
  # hexagon menu items
  - menu.yaml
  # hexagon articles
  - root.yaml

  - parties/parties.yaml
  - products/products.yaml
  - processes.yaml
  